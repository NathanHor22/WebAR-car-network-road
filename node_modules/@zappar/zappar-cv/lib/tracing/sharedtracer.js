import { PerformanceTracerManager } from "./performancetracer";
let sharedTracer = null;
export function getSharedTracer() {
    if (sharedTracer === null) {
        sharedTracer = new PerformanceTracerManager();
    }
    return sharedTracer;
}
